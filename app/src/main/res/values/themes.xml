<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="android:colorAccent">@color/primaryConteiner</item>
        <item name="colorPrimaryContainer">@color/primaryConteiner</item>
        <item name="colorOnPrimaryContainer">@color/black</item>
        <item name="colorOnSecondaryContainer">@color/white</item>

        <!-- Butonların metin rengi -->
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Pasif buton rengi -->
        <item name="android:textColorSecondary">@color/black</item>
        <!-- Navigation bar rengi -->
        <item name="android:navigationBarColor">@color/black</item>
        <!-- Bottom nav rengi -->
        <item name="bottomNavigationStyle">
            @style/Widget.MaterialComponents.BottomNavigationView.Colored
        </item>
        <item name="colorOnPrimary">@color/primaryConteiner</item>
        <item name="android:textColorPrimaryInverse">@color/primaryColor</item>

    </style>

    <style name="Theme.BudgetApp" parent="Base.Theme.BudgetApp" />

</resources>